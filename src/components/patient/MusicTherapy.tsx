// Music Therapy feature removed. This file kept as a harmless stub to avoid
// accidental import errors while we remove references gradually.

import React from 'react';

const MusicTherapy: React.FC<{ onBack?: () => void }> = () => {
  return null; // intentionally blank — feature removed
};

export default MusicTherapy;